# `mili-py` M4 — full `query()`

> Scope doc for Phase 2 Milestone 4. Builds on [`m1.md`](m1.md)
> (decisions 1–8), [`m2.md`](m2.md) (9–12), [`m3.md`](m3.md) (13–15)
> — all fixed. Honors the pinned FFI contract in
> [`../mili-rs/plan.md`](../mili-rs/plan.md) § "FFI integration plan".
> Read [`../mili-rs/status.md`](../mili-rs/status.md) § "Known gaps
> Phase 2 inherits" first — M4 is where those four gaps come due.

## Goal

`query()` reaches full upstream filter parity: every filter
combination (`material=`, `labels=`, `states=`, `ips=`, multi-svar)
and the hidden `**kwargs` knobs (`subrec`, `source`,
`reference_state`, `face`, `output_object_labels`), with the typed
error hierarchy surfaced cleanly. The component-name derivation in
core catches up to the M3-deferred forms. The exit gate is the
upstream read-path test suite running against `milox` with only an
import redirect, plus a broad milox filter-combination parity table.

## The literal-README tension (resolved here — decision 16)

README § "Phase 2 milestones" M4: *"Run `reference/mili-python/tests/`
unmodified against the new module."* Taken literally over the whole
directory this is **not achievable at M4 and never was scoped to
be**:

- `test_append_states*.py`, `test_modify_database.py`, the write
  half of `test_afileIO.py`/`test_miliinternal.py` exercise the
  **write path** — explicitly `status.md` § "Deferred to Phase 3".
- The suite does `from mili import reader`,
  `from mili.milidatabase import MiliPythonError, ResultModifier`,
  `from mili.datatypes import Superclass, Metadata`,
  `from mili.mdg_defines import EntityType, ...`, and drives the
  full `MiliDatabase` wrapper (`__postprocess` + `reductions.combine`,
  `modifier`, `project_to_nodes`, `as_dataframe`, derived) via
  `reader.open_database(..., suppress_parallel=, merge_results=,
  procs=)`.

The README's own § "Public surface" / § "Non-goals" already resolves
the tension: *the post-processing wrappers stay in Python over the
Rust primal call for v1*. So "unmodified" means **the read-path test
modules run with only an import redirect onto a `mili`-API-compatible
`milox` package** — not a from-scratch Rust reimplementation of the
Python wrapper layer, and not the Phase-3 write modules.

**Decision 16 — port the upstream Python wrapper layer into `milox`,
thin over the Rust core.** `milox` gains, ported essentially verbatim
from `reference/mili-python/src/mili/`:

- `milox.reader.open_database(...)` — the `suppress_parallel` /
  `merge_results` / `procs` surface (today's `open_database` shim
  grows these kwargs).
- a `MiliDatabase`-equivalent wrapper: `__postprocess` +
  `reductions.combine`, `ResultModifier` (`__process_query_modifier`),
  `project_to_nodes` (`__project_result`), `as_dataframe`
  (`result_dictionary_to_dataframe`), the derived layer
  (`derived.py`), and the `EntityType`/`Superclass`/`Metadata`/
  `mdg_defines` enums the tests import.
- the per-database object underneath is the Rust-backed
  `PyMiliDatabase` (slots in where upstream's `_MiliInternal` sits;
  the `LoopWrapper`/`ServerWrapper` fan-out stays collapsed into the
  Rust `DatabaseSet` per m1.md decision-shape — the wrapper's public
  method-forwarding surface is what the tests touch and it is
  preserved).

This keeps the m1–m3 invariant intact: **parity-sensitive
ordering/string/value logic lives in the Rust core; everything
ported into the Python layer is non-parity post-processing already
written and tested upstream.** The write-back half of `write_data`,
`append_state`, `copy_non_state_data`, `modify_database` stay
Phase 3 — out of M4. The exit gate runs the read-path modules
(`test_milidatabase.py` read tests, `test_bugfixes.py`,
`test_miliinternal.py` read tests, `test_derived.py`,
`test_projection.py`, `test_reductions.py`, `test_grizinterface.py`,
`test_reader.py`, `test_adjacency.py`) — write modules xfail/skip
with a Phase-3 reason, not a silent pass.

## In scope for M4

1. **Full primal filter combinations in the binding + core wiring.**
   - `material=` (name or number) — core already has
     `QueryArgs::materials` + `labels_for_materials`; the binding
     currently hardcodes `materials: None`. Wire it, plus the
     `labels` ∩ `material` intersection
     (`miliinternal.py:1060-1066`).
   - `labels=` single int or list; `states=` incl. negative indices
     (done m3) and `np.unique`/sort normalization
     (`miliinternal.py:1040-1045`); `ips=` single/list with
     `np.unique`; multi-svar; `output_object_labels`.
   - `**kwargs`: `subrec=` (filter `srecs_to_query` by name —
     `miliinternal.py:1246-1247`), `source=` (`'primal'`/`'derived'`
     routing — primal in core, derived routed to the ported Python
     derived layer), `reference_state=`, `face=` (passed through to
     derived/projection layer). Unexpected-kwargs →
     the upstream `ReturnCode.ERROR` contract surfaced as
     `MiliPythonError` (`miliinternal.py:1159-1163`).
2. **Component-name gaps closed in core** (`family.rs::svar_query_meta`
   / `expand_components`), matching `miliinternal.py:1362-1378`:
   - subscript form: `query("hx[3]")` → `components=["hx[3]"]`
     (today `svar_query_meta` strips `[` and re-expands the whole
     array — wrong). Single combined `f"{name}[{','.join(idxs)}]"`
     when a subscript is given.
   - ip-filtered vec-array: `f"{comp} ipt. {ip_label}"` per selected
     IP label (today `expand_components` ignores `ips`). Needs the
     resolved IP-label list threaded into `svar_query_meta` (the
     core gather already filters IPs — Step 11/10 — only the
     *naming* lags).
3. **Inherited Phase-2 gaps — tractability assessment (see below).**

## Out of scope (Phase 3 / no-fixture / blocked)

- Write-back: `write_data` write half, `append_state`,
  `copy_non_state_data`, `modify_database` — Phase 3.
- Partial-dim array subscript (`g[1]` on `dims=[3,4]`) — **no corpus
  fixture**; keep the typed `MiliError::Unsupported`. Implement when
  a real call hits it.
- Cross-srec-format multi-state — **no corpus fixture**
  (`srec_fmt_qty=1` universal); keep typed `Unsupported`.
- `Arc<Mmap>`/`PyCapsule` zero-decode — M5.

## Inherited-gap tractability (the bound-scope step)

| Gap | M4 verdict | Rationale |
|-----|-----------|-----------|
| **Aggregate VEC_ARRAY** (`query("es_1a","shell")`) | **Blocked for bit-exact parity; document + assert structurally** | mili-python raises `IndexError` on the same query (`status.md`), so there is no oracle for the aggregate form. mili-rs returns coherent data. M4 keeps core behavior, adds a `pytest.raises`/structural test asserting milox does *not* crash and the component-name path (`query("es_1a[1]")` / explicit component svars) *is* cross-validated. No upstream Python patch in M4 scope. |
| **Bare-component cross-material element-sets** (`query("sx","brick")` on basic1) | **Attempt in M4, split if it balloons (RESOLVED — user deferred to judgment)** | The contract (`InconsistentIpCounts` typed error, Step 16) is already built and waiting; `find_vector_parent` deliberately skips VEC_ARRAY parents. M4 extends bare-component resolution to consider VEC_ARRAY parents per-subrec (the `es_5a`/`es_7a` substitution mili-python does), composing the existing IP picker; the Step-16 typed error fires when substituted parents disagree on IP count. Highest-risk core change — gated behind its own parity row. If it needs a larger refactor than the `find_vector_parent` extension (re-assessed against basic1 during implementation), it splits to M4-followup and the reason is recorded here. |
| **Partial-dim array subscript** | **Out (no fixture)** | Keep typed `Unsupported`; documented above. |
| **Cross-srec-format multi-state** | **Out (no fixture)** | Keep typed `Unsupported`; documented above. |

If the bare-component core change proves to need a larger refactor
than the `find_vector_parent` extension (re-assessed during
implementation against basic1), it splits to M4-followup and M4
ships items 1–2 + the structural VEC_ARRAY handling; recorded as a
resolved decision here when known.

## Resolved decisions (to settle before coding)

16. **Exit gate scoped to filter-table parity + a targeted upstream
    redirect (RESOLVED — user deferred to judgment).** The full
    wrapper-layer port (derived/projection/dataframe — ~1900 test
    LOC, all non-parity post-processing already proven upstream) is
    bulk with low parity risk and is **M4-followup**, not M4. M4
    ships: (a) full primal filter combinations, (b) the
    component-name core fix, (c) the broad `milox` filter-combination
    parity table — the real exit proof — and (d) a **targeted
    redirect of `test_bugfixes.py`** (the mandated edge-case suite:
    small, read-path, no derived/dataframe deps) onto a minimal
    `mili`-compatible `milox` surface (`reader.open_database` kwargs
    + a thin `MiliDatabase`-equivalent over the Rust core). Write
    modules / the full wrapper port are out of M4, tracked as
    M4-followup with the Phase-3 boundary unchanged.
17. **Component-name derivation stays in core (M3 decision-13
    precedent), extended for subscript + ip-filtered forms.** The
    binding stays a pure pass-through; `svar_query_meta` gains the
    parsed-subscript and resolved-IP-label inputs it needs (it
    currently throws both away). No component-name logic in Python.
18. **Post-processing placement (README "Public surface" confirmed).**
    `as_dataframe`, `modifier` (ResultModifier),
    `project_to_nodes`, derived `source='derived'`, and the
    `reductions.combine` merge stay **Python-layer-over-primal**,
    ported from upstream verbatim. They are non-parity-sensitive
    (operate on the already-parity-correct primal `QueryDict`). The
    *read* assembly of `write_data` (shape echo) is supported; the
    *write-back* is Phase 3.

## Harness shape

Two layers:

1. **milox filter-combination parity table** —
   `crates/mili-py/tests/test_query_parity.py` extended: a per-case
   table over `conftest.CASES` (serial + xmilics) crossing
   representative svars with `{labels, material, states (incl. neg),
   ips, subrec, multi-svar}` combinations, asserting bit-exact vs
   upstream `mili` (`class_name`/`source`/`title`/`components`/
   `layout.labels`/`layout.states`/`data` dtype+shape+values,
   value-close `times`). Reuse the `parity_corpus.rs` pinned tuples
   as the seed set.
2. **Upstream read-path suite redirect** —
   `crates/mili-py/tests/upstream/` with a `conftest.py` that aliases
   `mili` → `milox`'s compatibility surface and a thin runner that
   collects the read-path modules from
   `reference/mili-python/tests/`, skip-not-fail when the submodule/
   oracle is absent (mirrors `conftest.py`). Write modules marked
   xfail(reason="Phase 3 write path").

## Slice A / Slice B split (RESOLVED during implementation)

The implementation surfaced a hard line between two bodies of work.
The user pre-authorized the contingency ("attempt in M4, split if it
balloons"); the evidence below splits it.

**Slice A — delivered in M4 (bit-exact cross-validated vs upstream
`mili`):**

- Core: `subrec=` filter plumbed through `Filter` /
  `collect_matching_subrecs` / `QueryArgs` (binding pass-through; new
  `query::tests::plan_subrec_filter_restricts_to_named_subrecord`).
- Core: `svar_query_meta` subscript form — `query("hx[3]")` →
  `["hx[3]"]`, `query("hx[8]")` → `["hx[8]"]`, bare `hx` → full
  `hx[1..=8]` (decision 17, subscript half). Proven bit-exact on
  `th/serial/d3samp6.th` vs upstream (components **and** data).
- Binding: `material=` (int / name / digit-string → numbers via the
  materials map), `labels=` (∩ `material` in core), `states=`
  (scalar / list / negative / `unique` / sorted), `ips=` (scalar /
  list / unique / sorted — pass-through; label semantics is Slice B),
  multi-svar (deduped, input order), `subrec=`, `**kwargs`
  validation → `MiliPythonError` (unexpected kw; `source!='primal'`,
  `reference_state`/`face`, `output_object_labels=False`,
  `write_data` all typed-errored with the Phase-3 / M4-followup
  reason). Verified vs upstream: vector / multi-state / negative /
  dedupe / `material` (1–4 on fdamp1) / `labels` / multi-svar /
  `subrec=1hex_mmsvn_rec` — all bit-exact (`test_query_parity.py`
  `test_filter_combination_parity`, 12 combos; + the Slice-A
  `test_bugfixes` value cases lifted verbatim).

**Slice B — split to M4-followup (recorded here, reason concrete):**
bare-component-of-**VEC_ARRAY** resolution (`sx`/`sy`/`syz`/`eps` on
`shell`/`brick`), the ip-as-**label** semantics (upstream `ips=` are
element-set IP *labels* matched via `__int_points`/`__element_sets`;
core `Filter.ips` is 0-based positional), the `f"{comp} ipt. {label}"`
component naming, and the cross-material `InconsistentIpCounts`
tightening (basic1 `sx`/`brick`, mat 5 = 8 IPs vs mat 7 = 9 IPs).
**Why split, not a `find_vector_parent` tweak:** the evidence shows
this needs (a) VEC_ARRAY-parent resolution *and* (b) a new
svar→element-set→IP-label linkage in core (the `__int_points`
recursive build has no `mili-rs` analogue) *and* (c) label→index
mapping *and* (d) cross-material per-subrec IP-count reconciliation —
four coupled core subsystems, well past the single extension the
decision-17 contingency scoped to M4. The exact upstream oracle
values (`test_bugfixes.py`) are encoded as
`xfail(strict=True)` cases (`test_bugfixes_slice_b_oracle`) so the
follow-up lands against a ready, self-tripping gate; the
cross-material **must-raise** contract is asserted today
(`test_bugfixes_cross_material_inconsistent_ips_contract`).

The full upstream-suite import-redirect + Python wrapper-layer port
(decision 16) is likewise M4-followup; M4 instead lifts the
cross-validatable `test_bugfixes` *values* directly into
`test_query_parity.py`, delivering that coverage without the port.

## M4 exit checklist

- [x] Binding: `material=`/`labels∩material`/`ips`/`subrec`/`source`/
      `output_object_labels`/multi-svar wired; `**kwargs` validation
      → `MiliPythonError`; negative/unique/sort state norm. (Slice A)
- [x] Core: `svar_query_meta` emits the subscript component name
      (decision 17, subscript half); ip-filtered `ipt.` form → Slice B.
- [x] Bare-component cross-material + `InconsistentIpCounts` —
      **M4-followup Slice B LANDED.** Core gained `query::IntPoints` +
      `Database::build_int_points` (the `__int_points` analogue),
      VEC_ARRAY-parent substitution (`plan_state_svar_ip` /
      `try_vec_array_substitution`, per-subrec component-outer/IP-inner
      pickers), `ips=` *label*→positional mapping, the
      `f"{comp} ipt. {label}"` naming (decision 17 ip-filtered half),
      and the cross-material per-subrec IP-count reconciliation firing
      Step-16 `InconsistentIpCounts`. Binding unchanged (thin
      pass-through). Strict-xfail removed — the 4 oracle cases now pass
      bit-exact; the must-raise contract + component-name test pass.
      The pre-encoded `basic1-sx-ip4` / contract fixture was repointed
      from `parallel/basic1` (no element sets, no Slice-B oracle there)
      to `serial/basic1` (upstream's own
      `InconsistantIntPointsForElementClassResult` base) — recorded in
      `status.md` § "Surprises".
- [x] Aggregate VEC_ARRAY documented blocked (upstream `IndexError`,
      no oracle); component-path closed by Slice B, aggregate kept as-is.
- [~] `milox` Python wrapper layer (decision 16/18) → still
      M4-followup item 2 (bulk, low-parity-risk, separate). Slice B
      (item 1, the core work) is done.
- [x] `test_query_parity.py` filter table bit-exact vs upstream
      (milox suite **262 passed**, no xfail; Slice B oracles green).
- [x] Rust workspace + `--features parity` green (no regression from
      the Slice B core changes); clippy/fmt clean.
- [x] `status.md` § "Surprises"/"Known gaps" updated.

## M4-followup item 2 — wrapper-layer port: RE-SCOPED (decision 19)

> Decision 16 framed the upstream-suite import-redirect + Python
> wrapper-layer port as *"bulk, low-parity-risk … ~1900 test LOC"*.
> Implementation investigation (post-Slice-B) shows that framing is
> **wrong on both counts** and the item is re-scoped here.

**The facts (measured against the pinned submodules):**

- The upstream read-path suite drives **~50 `_MiliInternal` public
  methods** through the `MiliDatabase` wrapper.
  `crates/mili-py/src/database.rs` (`PyMiliDatabase`) exposes **13**.
  The ~37-method gap is not "post-processing already proven
  upstream" — it includes genuinely new, **parity-sensitive**
  computation with no `mili-rs` analogue: the `derived.py` engine
  (2440 LOC, value-producing), `adjacency.py` (313),
  `projection.py` (172, `project_to_nodes`), `faces`,
  `nodes_of_elems` / `nodes_of_material`, `geometric_mesh_info.py`.
- Read-path **test** LOC is **~7300** (not ~1900):
  `test_milidatabase.py` 2604, `test_miliinternal.py` 794,
  `test_derived.py` 1907, plus projection/reductions/adjacency/
  grizinterface/reader.
- Even the smallest module (`test_reader.py`, 68 LOC) needs the full
  `mili`-compatible **module/class skeleton** (`MiliDatabase`,
  `_MiliInternal`/`LoopWrapper`/`ServerWrapper` identities,
  `reader.open_database` kwargs, `afileIO.MiliFileNotFoundError`)
  before any redirect can collect.

Decision 16's invariant ("everything ported into Python is non-parity
post-processing") **does not hold** for `derived`/`projection`/
`adjacency`/`faces`: those compute values and must be parity-validated
like the primal path. They belong in the **Rust core** (m1–m3
precedent), not "verbatim Python over primal".

### Decision 19 — phased plan (supersedes the single "M4-followup
item 2" line)

- **Phase F — Foundation (this commit).** The `mili`-API-compatible
  milox package skeleton, zero-parity-risk and complete:
  `milox.reader.open_database(base, procs, suppress_parallel,
  experimental, merge_results, **kwargs)` with upstream's
  handler-selection logic; verbatim pure-data ports
  (`milox.mdg_defines`, `milox.datatypes` →
  `Superclass`/`Metadata`/`ReturnCode`, `milox.afileIO.afiles_by_base`
  + `MiliFileNotFoundError`); `milox.parallel`
  (`LoopWrapper`/`ServerWrapper` markers — the Rust `DatabaseSet`
  collapses the real fan-out, these exist for isinstance/handler
  parity); `milox.milidatabase.MiliDatabase` thin wrapper +
  `milox.miliinternal._MiliInternal` adapter over `PyMiliDatabase`
  (serial `__postprocess` = identity, matching upstream single-
  fragment); a conftest **import-redirect harness** aliasing
  `mili*` → `milox`. **Green exit: redirected `test_reader.py`.**
  Unported `_MiliInternal` methods raise a typed
  `MiliPythonError("<name>: not yet ported (M4-followup phase N)")` —
  explicit, never a silent wrong answer. Existing 262 milox parity
  tests stay green (top-level `open_database` keeps working through
  the wrapper).
- **Phase G — primal-only read surface. ✅ LANDED (this change).**
  The `_MiliInternal` reshape methods (no new parity-sensitive math),
  all in the Rust core (`mili_rs::reshape`) + thin `database.rs`
  binding pass-throughs, each bit-exact vs. the upstream
  `_MiliInternal` oracle across the full serial corpus
  (`crates/mili-rs/tests/parity_reshape.rs`):
  `subrecords`, `state_variables`, `queriable_svars`,
  `classes_of_state_variable`, `state_variables_of_class`,
  `containing_state_variables_of_class`, `class_labels_of_material`,
  `all_labels_of_material`, `parts_of_class_name`,
  `materials_of_class_name`, `state_variable_titles`,
  `components_of_vector_svar`, `int_points_of_state_variable`,
  `mesh_object_classes`, `superclass_from_class_name`,
  `material_classes`, `parameters`, `parameter`, `srec_fmt_qty`
  (+ `metadata`, `reload_state_maps`, upstream-shaped
  `labels`/`times`/`state_maps`, and the upstream-API `_MiliInternal`
  constructor). The `StateVariable`/`Subrecord`/`MeshObjectClass`/
  `StateMap`/`MiliType` dataclasses are verbatim pure-data ports into
  `milox.datatypes`; the Rust core owns the reshape logic, the Python
  adapter only boxes. `_UNPORTED` now contains only Phase H.
  **Green:** redirected `test_reader` (7) **+ `test_miliinternal`
  (34 pass, 7 honest `xfail` for Phase H)**; full milox suite
  303 pass / 7 xfail; `cargo test --workspace --exclude mili-py
  --features parity` green (incl. the new `parity_reshape` sweep);
  clippy/fmt clean. Found + fixed a real `CLASS_DEF` parse bug
  (last-wins long_name; see status.md resolved-questions log).
  **Phase G close (read-half slice — LANDED).** `test_milidatabase.py`
  read half wired into the redirect harness. The `MiliDatabase`
  wrapper keeps the merge in Rust (decision 19 — `__postprocess` is
  identity, *not* a Python re-reduce) but now mirrors upstream's
  return-code raising: every forwarded accessor coerces
  `EntityType`/`StateVariableName` args via `mdg_enum_to_string`, calls
  the engine, then `returncode()` → `parse_return_codes` (raises
  `MiliPythonError`) → `clear_return_code()` (clear-before-raise, so a
  raised error never strands a stale code). `LoopWrapper`/
  `ServerWrapper` got OK-`returncode()`/no-op-`clear_return_code()`.
  `_MiliInternal` gained upstream-signature `query` (TypeError for a
  missing class / non-str class / non-iterable svar, matching
  upstream's arg-binding + return-code contract) and `connectivity`
  (ERROR on unknown class). Two **real core query gaps** surfaced via
  the oracle and were fixed in `mili_rs` with bit-exact parity
  (`tests/parity_component_subscript.rs`): (1) named-component
  subscript `parent[comp]` (`QueryName::CompSubscript`); (2) bare
  component of a multi-parent VECTOR disambiguated by subrec
  membership (`sx` on `brick`). **Green:** redirected `test_reader`
  (7) + `test_miliinternal` (34/7 xfail) + `test_milidatabase` read
  half; full milox suite 384 pass / 201 xfail; `cargo test
  --workspace --exclude mili-py --features parity` green; clippy/fmt
  clean. The parallel handler classes and the geometry / derived /
  projection / query-result-modifier serial methods are honest strict
  `xfail` (Phase H / parallel scope) — never silently passed. **Still
  Phase H after this slice** (the geometry sub-slice below closed
  `faces`/`nodes_of_elems`/`nodes_of_material`/`measure`/
  `connectivity_ids`): `derived_variables_of_class` + the rest of the
  derived engine, `project_to_nodes`, the `ResultModifier` reductions,
  and all per-proc-unmerged parallel-handler test classes.
- **Phase H — geometry/derived/projection/adjacency.**
  - **Geometry sub-slice ✅ LANDED (this change).** `connectivity_ids`,
    `nodes_of_elems`, `nodes_of_material`, `faces`, `measure` in the
    Rust core (`mili_rs::geometry`, thin `database.rs` +
    `milox.miliinternal._MiliInternal` bindings), each bit-exact vs the
    upstream `_MiliInternal` oracle across the serial corpus
    (`crates/mili-rs/tests/parity_geometry.rs`). `measure` is the
    self-contained `__compute_centroid` geometry over the primal
    `nodpos` query — **no** derived engine / projection / reductions
    (see status.md § "Surprises"). `connectivity_ids` added to
    `family.rs` next to `connectivity_labels`; `Superclass::node_count`
    added (verbatim from upstream `datatypes.py:87`). Removed from
    `_UNPORTED`; the 4 `test_miliinternal` (`test_faces`,
    `test_nodes_label`, `test_nodes_material`) + 4 serial
    `test_milidatabase` (`test_faces`, `test_nodes_of_elems`,
    `test_nodes_material`, `test_measure`) strict-xfails promoted to
    green (15 cases across serial variants). milox suite **399 pass /
    186 xfail**; `cargo test --workspace --exclude mili-py --features
    parity` green (incl. `parity_geometry`); clippy/fmt clean. No real
    corpus-vs-doc bug surfaced — the geometry methods were bit-exact on
    first parity run (the Phase-G `mesh.rs`/`query.rs` fixes already
    paid that debt).
  - **Adjacency + geometric-mesh-info sub-slice ✅ LANDED (this
    change).** The `_MiliInternal.geometry` (`GeometricMeshInfo`)
    surface — `compute_centroid`, `nearest_node`, `nearest_element`,
    `nodes_within_radius`, `elems_of_nodes` — and the serial
    `AdjacencyMapping` surface — `mesh_entities_within_radius`,
    `mesh_entities_near_coordinate`, `elems_of_nodes`, `nearest_node`,
    `nearest_element`, `neighbor_elements`, `neighbor_nodes`,
    `compute_centroid` — in the Rust core (new `mili_rs::adjacency`,
    `Superclass::node_connections` verbatim from `datatypes.py:91`),
    each bit-exact vs the upstream `_MiliInternal.geometry` /
    `adjacency.AdjacencyMapping` oracle across the serial corpus
    (`crates/mili-rs/tests/parity_adjacency.rs`).

    **GeometricMeshInfo placement decision (recorded per the task):
    thin adapter delegating to already-ported core accessors, *not* a
    verbatim Python port.** The architecture invariant is
    dispositive — `compute_centroid` / `nearest_*` /
    `nodes_within_radius` / `elems_of_nodes` are parity-sensitive
    value/coordinate/topology computation, so they live in the Rust
    core with bit-exact `parity_adjacency.rs` coverage (the task's
    explicit "(1) add bit-exact Rust parity coverage" requirement only
    makes sense with the math in core). `milox.geometric_mesh_info`
    and the new `milox.adjacency` are thin upstream-API-compatible
    adapters; the serial `AdjacencyMapping` `if not self.serial`
    branches collapse to identity (the Rust `DatabaseSet` already
    merges), so the adapter just forwards. Matches upstream's public
    surface exactly (`db.geometry` → `GeometricMeshInfo`,
    `adjacency.AdjacencyMapping(mili)`).

    `geometry` removed from `_UNPORTED`; `test_geometry_property`
    promoted out of `_XFAIL`; `test_adjacency.py` wired into the
    redirect harness (serial `TestSerialGeometricMeshInfo` /
    `TestSerialAdjacencyMapping` green, the 8 `LoopWrapper*` /
    `ServerWrapper*` parallel-handler classes honest strict-xfail like
    `_MDB_PARALLEL_CLASSES`). milox suite **411 pass / 225 xfail**;
    `cargo test --workspace --exclude mili-py --features parity` green
    (incl. `parity_adjacency`); clippy/fmt clean. Two real upstream
    bugs surfaced and scoped (not papered over): (a)
    `compute_centroid` on non-element / nodpos-uncovered entities
    raises an uncaught `IndexError` upstream (undefined oracle —
    sweep restricted to the oracle's `connectivity_ids().keys()` +
    nodpos-covered nodes); (b) `neighbor_nodes` on a BEAM's 3rd
    orientation node `IndexError`s upstream ((2,1) `node_connections`
    vs 3-col beam connectivity) — Rust mirrors it as a typed error,
    never a silent wrong answer. The genuine corpus-vs-doc parity
    invariants found are recorded in status.md § "Surprises"
    (nodpos-summation order; lenient `np.isin` query semantics;
    `pd.unique` first-occurrence order).
  - **Reductions + node-displacement derived sub-slice ✅ LANDED
    (this change).** The `ResultModifier` reductions
    (`MIN`/`MAX`/`AVERAGE`/`CUMMIN`/`CUMMAX`/`MEDIAN`/`STDDEV`),
    `as_dataframe` (`result_dictionary_to_dataframe`), and the
    `modifier=`/`as_dataframe=`/`project_to_nodes` `query()` post-
    process ported **verbatim** into `milox`
    (`MiliDatabase.query`/`_process_query_modifier`, new
    `milox.reductions` + `milox.utils`, `QueryDict`/`QueryLayout`
    added to `milox.datatypes`) — decision-18 non-parity post-
    processing over the parity-correct primal/derived result;
    `reductions.combine` is identity (the Rust `DatabaseSet` already
    merged — decision 19), never a Python re-reduce. **Decision 18's
    "operates on the already-parity-correct primal" premise held** —
    no reduction needed parity-sensitive Rust math. The catch
    (resolved per the user's "pull derived forward" call): every one
    of the 12 `test_milidatabase` reduction methods *also* queries
    `disp_x`/`disp_y` on `node` (`source='derived'`), so the
    node-displacement derived family was pulled into the **Rust core**
    (`mili_rs::derived`: `disp_<d> = u<d> − nodes()`-reference at
    `reference_state=0`; thin `database.rs` `source='derived'`
    routing) — parity-sensitive value computation, so core + bit-exact
    vs the `_MiliInternal` oracle (architecture invariant; decision
    19). A real core query bug surfaced + fixed (corpus wins): `ips=`
    on a non-VEC_ARRAY svar is now silently ignored to match upstream
    (was a no-oracle-basis `IpFilterNotApplicable`); see status.md §
    "Surprises". The 12 reduction methods promoted to green;
    `test_reductions.py` wired into the redirect harness
    (`TestSerialReductions` green; `TestCombineFunction` /
    `TestServerWrapperReductions` / `TestLoopWrapperReductions` /
    `TestMergeDataFrames` parallel-d3samp6 handler scope + the
    derived-*listing* / write methods honest strict-xfail). `test_query_average_dataframe`
    additionally promoted in the 4 `_MDB_PARALLEL_CLASSES` (partition-
    independent reduction = bit-identical merged). milox suite
    **473 pass / 269 xfail**; `cargo test --workspace --exclude
    mili-py --features parity` green; clippy/fmt clean.
  - **Derived-variable listing sub-slice ✅ LANDED (this change).**
    The derived-*listing* surface — `supported_derived_variables` /
    `derived_variables_of_class` / `classes_of_derived_variable` (plus
    `supported_variables` / `derived_variable_titles` /
    `find_batchable_queries`) — ported **verbatim** into a new
    `milox.derived.DerivedExpressions` over the already-ported core
    accessors (`classes_of_state_variable` / `mesh_object_classes` /
    `queriable_svars` / `class_names` / `clear_return_code`).
    **Placement decision (recorded per the task): verbatim Python
    metadata port, *not* Rust core.** Listing is a static expression
    table + set-membership over already-parity-correct metadata — not
    parity-sensitive value/topology computation — so the
    decision-18/19 precedent (reductions, the GeometricMeshInfo
    adapter) applies: it ports verbatim into `milox`. The
    `__derived_expressions` table is copied verbatim (titles / primals
    / primals_class / only_sclasses / alternate_primals /
    supports_batching); the value `compute_function`s are **not** this
    sub-slice — they are parity-sensitive value math bound for the Rust
    core (decision 19; node displacement already in `mili_rs::derived`)
    and are wired to an explicit typed-error stub (`__value_not_ported`
    → `MiliPythonError` naming the next sub-slice; never a silent wrong
    answer). `_MiliInternal` instantiates `DerivedExpressions(self)`
    and the three listing methods delegate with upstream's exact
    return-code contract (class-not-exist / var-not-exist → `ERROR`
    code → `MiliPythonError` via `parse_return_codes`). `_UNPORTED` is
    now empty. `milox.reader` re-exports `combine` (upstream does
    `from mili.reductions import *`; surfaced by wiring `test_derived`,
    which imports it). `test_derived.py` wired into the redirect
    harness: `SerialDerivedExpressions` listing 3 + `disp_x`/`disp_y`/
    `disp_z` (already in core from the prior sub-slice — the strict
    harness flagged the incidental pass, promoted) green; every other
    `test_derived` method honest strict-xfail (derived *value* engine =
    next sub-slice; `ParallelDerivedExpressions` = that + parallel
    handler scope). The three listing strict-xfails in
    `test_miliinternal` / `test_milidatabase` / `test_reductions`
    promoted to green. No corpus-vs-doc bug surfaced — the verbatim
    table + listing logic was bit-identical to upstream's expected
    lists on first run. milox suite **488 pass / 336 xfail**; `cargo
    test --workspace --exclude mili-py --features parity` green;
    clippy/fmt clean.
  - **Nodal displacement-magnitude + `reference_state` sub-slice ✅
    LANDED (this change).** Extended `mili_rs::derived` (the
    architecture invariant — parity-sensitive value math in the Rust
    core, decision 19; "extend the node-displacement work, don't
    restart it") with `disp_mag` (`sqrt(dx²+dy²+dz²)`),
    `disp_rad_mag_xy` (`sqrt(dx²+dy²)`), and non-zero `reference_state`
    for the whole nodal-displacement family. The shared
    `__get_nodal_reference_positions` split into two core helpers:
    `nodal_reference_from_coords` (`reference_state==0` → initial
    `nodes()` by primal-label ordinal, the prior path refactored out)
    and `nodal_reference_from_query` (non-zero → the primal `u<dir>`
    query at that state, aligned to the primal labels by id);
    `compute_node_displacement` now takes the precomputed reference,
    `compute_node_displacement_magnitude` does the sqrt-of-sum-sq over
    the requested direction set. `database.rs` extracts the
    `reference_state` kwarg (was a hard typed error — now only `face`
    is, projection scope), routes the magnitude specs, and issues the
    internal reference query when `reference_state != 0`. milox is
    unchanged — `reference_state` already flowed through
    `MiliDatabase.query`'s `**kwargs` → `_MiliInternal.query` → the
    Rust binding. Bit-exact vs upstream's own oracle values via the
    redirect harness (`test_derived.py`'s hard-coded mili-python
    `assertAlmostEqual` targets; the established derived parity gate —
    the prior node-disp sub-slice used the same, no separate
    `parity_*.rs`). `test_disp_mag` / `test_disp_mag_ref` /
    `test_disp_rad_mag_xy` / `test_disp_y_reference_state` promoted to
    green (the strict harness flagged all four the moment the core math
    landed). No corpus-vs-doc bug — bit-identical on first run. milox
    suite **492 pass / 332 xfail**; `cargo test --workspace --exclude
    mili-py --features parity` green; clippy/fmt clean.
  - **Nodal velocity / acceleration sub-slice ✅ LANDED (this
    change).** Extended `mili_rs::derived` (parity-sensitive value
    math in the Rust core, decision 19) with the finite-difference
    kinematics: `vel_<dir> = (u(s) − u(s−1)) / (t(s) − t(s−1))` (zero
    at state 1) and `acc_<dir>` central difference with forward
    (state 1) / backward (last state) stencils
    (`derived.py.__compute_node_{velocity,acceleration}`). Generic
    over **f32 and f64** primals — `test_vel_*` opens the
    double-precision `serial/solids014/solids014_dblplt` (f64) while
    `test_acc_*` uses single-precision `d3samp6.plt` (f32); the
    time-derived factor is computed in **f32** (upstream `db.times()`
    is the f32 per-state header; numpy NEP50 keeps `1.0/f32` and
    `0.5*f32` in f32) then promoted to the primal dtype for the final
    multiply — exactly numpy's `f64_disp * f32_factor → f64` /
    `f32_disp * f32_factor → f32`. `2*u` written `u+u` (exact IEEE
    doubling, bit-identical, no cast). `database.rs` gathers the
    primal at every state the stencil touches in one query (the
    needed-state set built per requested state) and the core does the
    difference math; milox unchanged. Bit-exact vs upstream's own
    oracle via the redirect harness (`test_derived.py`'s hard-coded
    mili-python `assertAlmostEqual` targets). `test_vel_x/y/z` +
    `test_acc_x/y/z` promoted to green (the strict harness flagged all
    six the moment the core math landed). No corpus-vs-doc bug —
    bit-identical on first run. milox suite **498 pass / 326 xfail**;
    `cargo test --workspace --exclude mili-py --features parity` green;
    clippy/fmt clean.
  - **Scalar stress-invariants sub-slice ✅ LANDED (this change).**
    Extended `mili_rs::derived` (parity-sensitive value math in the
    Rust core, decision 19) with the four **non-eigenvalue** stress
    invariants — `pressure = (-1/3)(sx+sy+sz)`,
    `eff_stress = sqrt(3·J2)`, `triaxiality = -p/seff`,
    `norm_press = p/seff` (`derived.py.__compute_{pressure,
    effective_stress,triaxiality,normalized_pressure}` ~1467-1671) —
    pure element-wise arithmetic over the 6 stress component primals
    (`sx/sy/sz/sxy/syz/szx`; `pressure` reads only the 3 normals) on
    the **requested element class** (brick/shell/beam, IP-resolved by
    the existing `query_full` component path — a different shape from
    the nodal family, but the gather already handles bare-component +
    `ips`). **The cut: the scalar invariants only.** The
    principal-stress / principal-dev-stress / max-shear-stress family
    and the strain analogues all need `np.linalg.eigvalsh` of a
    symmetric 3×3 — a distinct numerical unit shared across both the
    stress *and* strain principal families — so isolating the
    eigensolver as its own next cut keeps this slice small, coherent,
    and zero-eigensolver-risk (the smallest clean line in the
    stress/strain cluster; `vol_strain`/`prin_strain*` are the
    immediately-following cut, with the eigensolver). Generic over
    f32 (`d3samp6.plt`) and f64 with numpy NEP50 weak-scalar
    promotion: the Python-float `-(1/3)` / `0.5` / int `3` are weak
    scalars cast to the array dtype, mirrored by **type-annotated `$t`
    literals** (`let third: $t = -1.0/3.0;`) — no `as` cast, and
    f32 `-1.0/3.0` is bit-identical to f64(-1/3) cast to f32; `x**2`
    is numpy's `fast_scalar_power` short-circuit to `x*x` (exact);
    op order left-associative exactly as the Python source evaluates.
    The `eff_stress` whole-array hydrostatic `np.allclose(sx,sy,sz,
    rtol=0,atol=1e-15) → -sx` collapse is mirrored as a global
    reduction (the `and` short-circuits); `triaxiality`/`norm_press`
    use the plain pressure (no collapse — matches the source).
    `database.rs` routes `source='derived'`, gathers each component
    primal with the request's own labels/states/ips, and the core does
    the math. **One binding-shape fix (no regression):** the QueryDict
    atom axis is now derived from the flat `[state][label][atom]`
    length, not `len(components)` — upstream
    `__initialize_result_dictionary` sets `components=[result_name]`
    over `np.empty_like(primal)`, so a multi-IP stress query keeps its
    per-IP 3rd axis while `layout['components']` stays `['pressure']`;
    falls back to `len(components)` only when the entity/state axes are
    empty. Equivalent for every primal / nodal-derived path (atoms ==
    `len(components)` there), so all prior-green tests unchanged.
    milox unchanged. Bit-exact vs upstream's own oracle via the
    redirect harness (`test_derived.py`'s hard-coded mili-python
    `assertAlmostEqual` targets — deltas tighter than the f32 ulp at
    these magnitudes, which forces exact numpy op-order, not just
    "close"). `test_pressure` / `test_eff_stress` / `test_triaxiality`
    / `test_normalized_pressure` / `test_query_multiple_variables`
    promoted to green (the strict harness flagged all five the moment
    the core math landed). No corpus-vs-doc bug — bit-identical on
    first run. milox suite **503 pass / 321 xfail**; `cargo test
    --workspace --exclude mili-py --features parity` green; clippy/fmt
    clean.
  - **Eigenvalue stress-invariants sub-slice ✅ LANDED (this
    change).** Extended `mili_rs::derived` (parity-sensitive value
    math in the Rust core, decision 19) with the seven
    **eigenvalue-based** stress invariants — `prin_stress1-3`,
    `prin_dev_stress1-3`, `max_shear_stress`
    (`derived.py.__compute_{principal_stress,principal_dev_stress,
    max_shear_stress}` ~1422-1607). Each assembles the symmetric
    stress 3×3 (`prin_stress*`) or the deviatoric 3×3 (diagonal
    shifted by `pressure = -(1/3)(sx+sy+sz)`; `prin_dev_stress*` /
    `max_shear_stress`) from the same 6 component primals on the
    requested element class as the scalar cut, reads `np.linalg.
    eigvalsh`, and extracts max / mid / min (ascending) or
    `0.5*(max-min)`. **Feasibility decision (recorded — no escalation
    needed):** LAPACK's exact `?syevd` bits aren't reproducible from a
    hand-written solver, *but* an empirical sweep of the upstream
    oracle proved a **f64 cyclic-Jacobi symmetric-3×3 eigensolver
    cast to f32 is bit-identical to numpy's native f32 `eigvalsh` at
    every literal-checked test point** across brick/shell/beam and all
    IP configs (48/48 final-derived-value checks vs the hard-coded
    `test_derived` literals+deltas pass; the deltas were written to
    absorb the Griz-C vs numpy spread, leaving ample margin). So the
    eigensolver lives in core (`jacobi_eigvalsh_sym3`, fixed 50-sweep
    / `1e-300`-cutoff / `copysign`-tangent schedule — the exact
    schedule validated): matrix assembled in the primal dtype (numpy's
    entries; NEP50 weak-scalar `-(1/3)` / `0.5`), promoted to f64,
    eigensolved, eigenvalues cast back to the primal dtype. The
    eigensolver is the new numerical unit isolated to one input family
    (the strain eigvalsh family reuses it next). `database.rs` routes
    `source='derived'` and gathers the 6 primals with the request's
    own labels/states/ips (same pattern as the scalar cut). milox
    unchanged. `test_prin_stress1/2/3` / `test_prin_dev_stress1/2/3`
    / `test_max_shear_stress` promoted to green (the strict harness
    flagged all seven the moment the core math landed). No
    corpus-vs-doc bug. milox suite **510 pass / 314 xfail**; `cargo
    test --workspace --exclude mili-py --features parity` green;
    clippy/fmt clean.
  - **Strain-invariants sub-slice ✅ LANDED (this change).** Extended
    `mili_rs::derived` (parity-sensitive value math in the Rust core,
    decision 19) with the seven non-`*_alt` strain invariants —
    `vol_strain` (trivial strain trace `ex+ey+ez`,
    `derived.py.__compute_vol_strain` ~1157), `prin_strain1-3` /
    `prin_dev_strain1-3` (`__compute_{principal,dev_principal}_strain`
    ~1174-1340). The principal strains **reuse the symmetric-3×3
    Jacobi eigensolver** landed in the prior sub-slice
    (`jacobi_eigvalsh_sym3`) on the 6 strain components — `prin_strain*`
    eigensolve the raw strain matrix, `prin_dev_strain*` the deviatoric
    one (diagonal shifted by the hydrostatic strain `e_hyd =
    (1/3)(ex+ey+ez)`; the source spells it `e - e_hyd` with *positive*
    1/3, vs stress's `s + (-1/3)·trace` — algebraically and
    bit-for-bit identical `component - (1/3)·trace`, but mirrored as
    written). Matrix assembled in the primal dtype (NEP50 weak-scalar
    `1/3`), promoted to f64, eigensolved, eigenvalues cast back —
    identical machinery to the stress family. Empirically verified
    bit-for-bit vs the upstream oracle: **53/53** final-derived-value
    checks (`vol_strain` + `prin_strain1-3` + `prin_dev_strain1-3`
    across brick/shell, all IP configs) vs the hard-coded
    `test_derived` literals+deltas pass. `database.rs` gathers the
    strain primals (3 for `vol_strain`, 6 otherwise) with the
    request's own labels/states/ips, same routing as the stress cuts;
    milox unchanged. `test_vol_strain` / `test_prin_strain1/2/3` /
    `test_prin_dev_strain1/2/3` promoted to green (the strict harness
    flagged all seven). No corpus-vs-doc bug. milox suite **517 pass
    / 307 xfail**; `cargo test --workspace --exclude mili-py
    --features parity` green; clippy/fmt clean.
  - **Magnitude-derived sub-slice ✅ LANDED (this change).** Extended
    `mili_rs::derived` with `shear_magnitude = sqrt(qxx²+qyy²)` and
    `nodtangmag = sqrt(nodtang_x²+nodtang_y²+nodtang_z²)`
    (`derived.py.__compute_{shear_magnitude,
    nodal_tangential_traction_magnitude}` ~2427 / ~1742) — the
    sqrt-of-sum-of-component-squares pattern, identical machinery to
    `disp_mag` / `eff_stress` (generic f32/f64, squares summed
    left-associatively as the source spells it, `**2` → `x*x`, no
    scalar constants so no NEP50 to track). Empirically oracle-verified
    bit-for-bit (24/24 checks: `shear_magnitude` f32 d3samp6,
    `nodtangmag` f64 dbl_nodtang at the 1e-20 literal deltas).
    `database.rs` gathers the component primals with the request's own
    labels/states/ips, same routing as the scalar cuts; milox
    unchanged. `test_shear_magnitude` promoted to green.
    **`test_nodetangmag` stays honest strict-xfail** with a precise
    reason: the magnitude math is implemented + verified, but the core
    *primal* query raises `label 95 not found on class cbs1_particle`
    while upstream mili returns data for it — a **pre-existing core
    label/class-membership divergence on the dbl_nodtang corpus,
    surfaced (not caused) by this cut**. Per the architectural
    guardrail this is flagged, not papered over: it is a
    settled-core-query-semantics investigation (label resolution on a
    specific corpus's class), independent of the derived value engine,
    and out of scope for a derived-math cut. milox suite **518 pass /
    306 xfail**; `cargo test --workspace --exclude mili-py --features
    parity` green; clippy/fmt clean.
  - **Derived value-engine completion sub-slices ✅ LANDED (this
    change).** Resolved the remaining value-tested derived families,
    each oracle-validated bit-for-bit *before* implementation:
    - `eps_rate` — single-primal finite-difference over states
      (`__compute_plastic_strain_rate`), mirrors the kinematics
      pattern; per-IP row width preserved (9/9 oracle checks).
    - `centroid` (hex/beam/shell/node) — new
      `geometry.rs::centroid_query` reusing the landed bit-exact
      per-label centroid (`measure` path).
    - `element_volume` (M_HEX 12-term Griz / M_TET `w·(u×v)/6`) +
      `area` (M_QUAD surface metric) — new
      `geometry.rs::elem_node_gather` mirrors upstream
      `nodes_of_elems` + `__generate_elem_node_map` + the single
      `nodpos` query; bit-exact `hex_volume`/`tet_volume`/quad-area
      (24/24).
    - `relative_volume` — `det F` deformation-gradient determinant
      over the state-0 reference Jacobian, f64 internal exactly as
      upstream's `np.float64` arrays, M_TET `[0,1,2,2,3,3,3,3]`
      node-expansion (`test_tet_relative_volume` green).
    - contact `normal_force` / `force_x/y/z` — cross-derived
      (`contact primal * the area derived`, label-aligned, dyna
      `sn`/`s1..s3`); 6/6 oracle checks, exact match.
    - `mat_cog_disp_x/y/z` — `matcg<d>(s) − matcg<d>(ref)`; upstream's
      reference_state default is **state 1** (binding tracks whether
      the kwarg was explicitly given).
    All `database.rs source='derived'` routed; milox unchanged.
    Promoted: `test_eps_rate`, `test_{hex,beam,shell,node}_centroid`,
    `test_{hex,tet}_element_volume`, `test_quad_area`,
    `test_tet_relative_volume`, `test_dyna_normal_force`,
    `test_force_{x,y,z}`, `test_mat_cog_disp` (+ `test_shear_magnitude`
    from the prior cut). milox suite **532 pass / 292 xfail**; `cargo
    test --workspace --exclude mili-py --features parity` green;
    clippy/fmt clean.
  - **dbl_nodtang core primal-query label-resolution bug ✅ FIXED
    (this change).** The systematic core *primal-query* bug on the
    dbl_nodtang (diablo) corpus is resolved at the root. **Root
    cause:** the labels-filtered gather binned the *user label*
    straight into a subrecord's `id_blocks`, but `id_blocks` enumerate
    1-based class **mesh-object ids** (ordinals), not user labels —
    they coincide only when a class has contiguous `1..=qty` labels,
    which is why every prior corpus hid it. `cbs1_particle` has labels
    `[5,10,..,125]` over mo ids `1..=25`, so label 5 silently read the
    wrong object and labels 95/115 fell outside `(1,25)` →
    `LabelNotFound`. **Fix** (`family.rs::run_query`): map the
    requested labels into the mo-id space before planning, mirroring
    upstream `np.where(np.isin(labels_of_class, labels))[0]`
    (`miliinternal.py:1183`) — class-label-array (ascending) order,
    absent-from-class labels silently dropped — via the new
    `Database::labels_to_mo_ids`; both the filtered and unfiltered
    gather paths now emit mo ids, so the entity axis is uniformly
    mapped back through `map_mo_ids_to_labels`. This is a **broad
    correctness fix** (any non-contiguous-label class on any corpus,
    not just dbl_nodtang). Bit-exact vs the upstream oracle on
    dbl_nodtang `cbs1_particle` incl. the ascending-reorder semantics
    (`[115,5]→[5,115]`, `[95,5,115]→[5,95,115]`). No regression: milox
    **533 pass / 291 xfail** (was 532/292 — `test_nodetangmag`
    promoted; it is a pure sqrt-of-squares magnitude with no internal
    geometry query); `cargo test --workspace --exclude mili-py
    --features parity` green; clippy/fmt clean.
  - **Still Phase H — genuine blockers (flagged, not forced):**
    1. **`test_hex_relative_volume` / `test_diablo_normal_force` —
       second, distinct blocker (geometry-derived f32-only).** With
       the label bug fixed these now fail one layer deeper:
       `mili_rs::geometry` hardcodes f32 for its internal `nodpos`
       query (`MiliAParseError('nodpos query returned a non-f32
       buffer')`), but dbl_nodtang is double-precision (f64 nodpos).
       This is a **separate, parity-sensitive "generalize
       geometry-derived to f64" sub-slice** (numpy op-order / NEP50
       over the volume / area / jacobian / centroid math) —
       independent of the now-fixed label-resolution bug *and* of the
       derived value engine. Honest strict-xfail with the accurate
       reason; recommended as its own coherent cut (sized like one of
       the derived value-engine sub-slices: a `$t`-generic geometry
       macro + bit-exact f64 oracle validation).
    2. `surfstrain*` additionally requires the `face=` **projection
       layer** (task-flagged projection scope) and runs on the
       dbl_nodtang corpus — deferred.
    3. The strain `*_alt` griz closed-form variants
       (`prin_strain*_alt`/`prin_dev_strain*_alt`) have **NO
       value-test** (listing-only, already green) — no forcing
       function, not implemented speculatively.
    Plus `projection.py` (`project_to_nodes`) and all
    per-proc-unmerged parallel-handler classes. Green target:
    `test_derived` (value — now complete except the blockers above),
    `test_projection`, `test_grizinterface`.
- **Phase 3 boundary unchanged.** `test_append_states*`,
  `test_modify_database`, the write halves stay write-path /
  Phase 3 — harness marks them `xfail(reason="Phase 3 write path")`.

Slice B (item 1, the parity-critical core work) is **done**;
Phase F is the foundation for the rest. Phases G/H are tracked here,
not bundled — each is its own parity-validated change set.
