# v1 SFT corpus — dataset card

Generated by `mili-llm-bench assemble` from Stage 5 teacher rollouts.
See `planning/mili-viz/mili-agent/m5-sft-pipeline.md` Stage 6 row and
`planning/mili-viz/mili-agent/posttraining-dataset.md` §6 for the
build-order context.

## Stage 6 decisions (preamble)

Three decisions were locked before the assembler ran. They live here
so future-you can reproduce the corpus without re-deriving them.

1. **Per-intent ≥10 floor (revised from the rev-4 ≥40).** The
   original ≥40 gate assumed scenario synthesis would emit a ~3×
   paraphrase multiplier. The actual `data/posttraining/scenarios/synth.jsonl`
   carries one canonical instruction per scenario, so after K=3@T=0.7
   dedup the retained corpus is **171 unique trajectories
   distributed across 14 intents (p50 ≈ 14, p25 ≈ 6, p75 ≈ 18)** —
   no intent can clear ≥40 without re-running synth. Re-pinned to
   ≥10 as the realistic v1 floor; under-floor intents flagged
   below as **v1 holes** with `TODO(v2)` to regenerate synth with
   `paraphrase_count > 1`.
2. **Per-intent held-out split (rather than whole-fixture).** For
   each intent, the smaller of its two `(intent, fixture)` cells is
   held out in full (tiebreak by alphabetical fixture name). Cleaner
   coverage of intents in eval at the cost of fixture coverage; the
   whole-fixture alternative needs a third fixture (shell_mat2 or
   bar5) bound through Stage 3 + Stage 5, which is a separate session.
3. **Compound family ratio enforced in both train AND heldout** at
   ≥20%. The assembler fails loud if either side drops
   below the gate.

`query_policy='accept'`. The 4 v6/v7 stage-6.5 `query`
failures (synth-00124/00127/00130/00133, all `wrong_final_state`,
parked under catalog `query.todo_v2`) are present in the upstream
rollouts only via the 8 passing scenarios; the assembler does not
re-roll. Setting `query_policy=drop` removes the intent entirely;
`accept` lands it as an under-floor cell.

`seed=42` controls the stratified train/val split. Heldout cell
choice is deterministic from the per-cell row counts (with an
alphabetical fixture tiebreak), independent of seed.


## Inputs

- `/p/vast1/whitmore/cadsat/mili-rs/data/posttraining/runs/stage5-fullsweep-anthropic-20260524-223426/rollouts.jsonl`

## Corpus counts

| Bucket | Rows |
| --- | ---: |
| input rollouts (post `query_policy`) | 525 |
| retained (max_tier == 3) | 513 |
| unique trajectories (instruction-aware dedup) | 171 |
| sft/train.jsonl | 82 |
| sft/val.jsonl | 8 |
| eval/heldout.jsonl | 81 |
| pref/*.jsonl (DPO pairs) | 0 |

## Compound ratio (≥20% gate)

- train: **22.0%**
- heldout: **24.7%**

## Per-intent row counts (train + val + heldout)

Floor = **≥10** on `sft/train.jsonl`. Under-floor cells are tagged as v1 holes.

| Intent | Pre-split | Train | Val | Heldout | Under floor |
| --- | ---: | ---: | ---: | ---: | :---: |
| clrsel | 6 | 4 | 0 | 2 | **yes** |
| colormap | 18 | 8 | 1 | 9 | **yes** |
| compound-material-then-show | 13 | 6 | 1 | 6 | **yes** |
| compound-select-then-show | 14 | 6 | 1 | 7 | **yes** |
| compound-state-then-show | 14 | 6 | 1 | 7 | **yes** |
| load | 6 | 3 | 0 | 3 | **yes** |
| material | 20 | 11 | 1 | 8 | no |
| query | 8 | 4 | 0 | 4 | **yes** |
| select | 16 | 7 | 1 | 8 | **yes** |
| set-state | 18 | 8 | 1 | 9 | **yes** |
| show-derived | 8 | 5 | 0 | 3 | **yes** |
| show-primal | 18 | 8 | 1 | 9 | **yes** |
| step | 8 | 4 | 0 | 4 | **yes** |
| view-reset | 4 | 2 | 0 | 2 | **yes** |

## Held-out split (per-intent policy)

| Intent | Choice |
| --- | --- |
| clrsel | heldout=cylinder (2 rows), train=d3samp6 (4 rows) |
| colormap | heldout=cylinder (9 rows), train=d3samp6 (9 rows) |
| compound-material-then-show | heldout=cylinder (6 rows), train=d3samp6 (7 rows) |
| compound-select-then-show | heldout=cylinder (7 rows), train=d3samp6 (7 rows) |
| compound-state-then-show | heldout=cylinder (7 rows), train=d3samp6 (7 rows) |
| load | heldout=cylinder (3 rows), train=d3samp6 (3 rows) |
| material | heldout=cylinder (8 rows), train=d3samp6 (12 rows) |
| query | heldout=cylinder (4 rows), train=d3samp6 (4 rows) |
| select | heldout=cylinder (8 rows), train=d3samp6 (8 rows) |
| set-state | heldout=cylinder (9 rows), train=d3samp6 (9 rows) |
| show-derived | heldout=cylinder (3 rows), train=d3samp6 (5 rows) |
| show-primal | heldout=cylinder (9 rows), train=d3samp6 (9 rows) |
| step | heldout=cylinder (4 rows), train=d3samp6 (4 rows) |
| view-reset | heldout=cylinder (2 rows), train=d3samp6 (2 rows) |

## (intent, fixture) cell coverage

| Intent | Fixture | Train rows | Heldout rows |
| --- | --- | ---: | ---: |
| clrsel | cylinder | 0 | 2 |
| clrsel | d3samp6 | 4 | 0 |
| colormap | cylinder | 0 | 9 |
| colormap | d3samp6 | 8 | 0 |
| compound-material-then-show | cylinder | 0 | 6 |
| compound-material-then-show | d3samp6 | 6 | 0 |
| compound-select-then-show | cylinder | 0 | 7 |
| compound-select-then-show | d3samp6 | 6 | 0 |
| compound-state-then-show | cylinder | 0 | 7 |
| compound-state-then-show | d3samp6 | 6 | 0 |
| load | cylinder | 0 | 3 |
| load | d3samp6 | 3 | 0 |
| material | cylinder | 0 | 8 |
| material | d3samp6 | 11 | 0 |
| query | cylinder | 0 | 4 |
| query | d3samp6 | 4 | 0 |
| select | cylinder | 0 | 8 |
| select | d3samp6 | 7 | 0 |
| set-state | cylinder | 0 | 9 |
| set-state | d3samp6 | 8 | 0 |
| show-derived | cylinder | 0 | 3 |
| show-derived | d3samp6 | 5 | 0 |
| show-primal | cylinder | 0 | 9 |
| show-primal | d3samp6 | 8 | 0 |
| step | cylinder | 0 | 4 |
| step | d3samp6 | 4 | 0 |
| view-reset | cylinder | 0 | 2 |
| view-reset | d3samp6 | 2 | 0 |

## Contamination check

Scenario IDs in `eval/heldout.jsonl` are disjoint from `sft/train.jsonl` ∪ `sft/val.jsonl`: **clean**.
`(intent_id, fixture)` cells in heldout are disjoint from train+val: **clean** (enforced by the split partitioner).

## Under-floor intents (v1 holes)

- **clrsel** — train rows = 4 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.
- **colormap** — train rows = 8 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.
- **compound-material-then-show** — train rows = 6 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.
- **compound-select-then-show** — train rows = 6 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.
- **compound-state-then-show** — train rows = 6 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.
- **load** — train rows = 3 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.
- **query** — train rows = 4 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.
- **select** — train rows = 7 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.
- **set-state** — train rows = 8 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.
- **show-derived** — train rows = 5 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.
- **show-primal** — train rows = 8 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.
- **step** — train rows = 4 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.
- **view-reset** — train rows = 2 (< 10). `TODO(v2)`: regenerate synth with `paraphrase_count > 1` so this intent has more retained trajectories under the same K=1 reroll.

## TODO(v2) — deferred for v1 pilot

- **Paraphrase multiplier in synth.** Re-run `mili-llm-bench synth` with `paraphrase_count > 1` per (intent, fixture, params) tuple to push every intent over the ≥40 floor. Cheapest path to lifting the under-floor intents.
- **K policy.** Drop K to 1 for the next sweep against a frontier teacher (rev-12 finding: 0/175 K=3 diversity at T=0.7). Reserve K > 1 for genuinely stochastic teachers (local 7B, T ≥ 1.0).
- **Whole-fixture held-out.** Bind a third fixture (`shell_mat2` or `bar5`) through Stage 3 + Stage 5 so heldout measures cross-fixture generalization, not the smaller intra-fixture cell.
- **Near-dup instruction filter.** Embedding / MinHash filter on `normalized_instruction` to catch paraphrase collapse. Not needed at 171 rows; becomes useful at v2 scale (~1k).
- **`query.todo_v2`.** The 4 `wrong_final_state` failures need either verifier leniency on default state, instruction pinning `state 1`, or a tool-schema default. Carried from rev 7.
- **DPO data.** v1 pref/*.jsonl is empty because K=3@T=0.7 produced zero mixed-tier scenarios. v2: rerun a subset at T ≥ 1.0 specifically to harvest (chosen, rejected) pairs.
