"""Dispatchers — Dispatcher Protocol implementations.

Each module here is one concrete dispatcher. ``pygriz`` is the
production lowering (lazy-imports the optional ``pygriz`` package);
tests use the ``FakeDispatcher`` in ``mili_llm_bench.harness``.
"""
